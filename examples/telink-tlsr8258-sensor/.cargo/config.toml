[build]
# TLSR8258 uses Telink's tc32 ISA. For cargo check, we use thumbv6m-none-eabi
# as a compilation stand-in. Real builds use Telink's tc32 GCC toolchain.
target = "thumbv6m-none-eabi"

[unstable]
build-std = ["core", "alloc"]

[env]
DEFMT_LOG = "info"
